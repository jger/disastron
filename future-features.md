# Future features

- Transfer offline LLM model files between devices (Bluetooth or another nearby/channel UX), so users can side-load once and share large installs without redownloading on metered networks.
