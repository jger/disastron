- readme: name etymology (δις + ἄστρον ↔ disaster via Latin dis- + astrum / Italian disastro); features + mission; open-source-when-ready; tech blurb
- android-gradle: Gradle 8.13, AGP 8.13.1, Kotlin 2.3.20; app JVM 17; app `compileSdk 36`; subprojects library `compileSdk 36` (androidx.core 1.17 / background_downloader)
- android-r8: `app/proguard-rules.pro` `-dontwarn` MediaPipe proto stubs (release R8; matches AGP `missing_rules.txt`)
- android-16kb: `ndkVersion 28.2.13676358`; manifest `android:pageSizeCompat=enabled`; APK check: offending ELF `libLiteRtLm.so` (`max_load_align=0x1000`) from flutter_gemma native assets -> runs in compat mode until upstream 16KB-aligned build
- flutter_gemma: ^0.14.1; main `FlutterGemma.initialize(huggingFaceToken: prefs)` when saved; no bundled `model.bin` (assets/images only)
- hf-token: `HuggingfaceToken.save`/`clear` → `FlutterGemma.initialize(huggingFaceToken: …)`; `installFromNetwork` passes saved/override token
- presets: `predefined_models` — **Gemma only** (no Qwen presets); Gemma 3 + Gemma 4 `.litertlm`; HF-gated per model type/default; `kSmallestStorageDownloadPreset` = Gemma 270M q8
- model-ui: `ModelSetupWidget` — **model library** (installed: Use / Active / **Save copy** / remove) + active card; preset: HF token + **Available downloads**; gated when token saved; `coordinateInferenceNetworkInstallPreflight` + `installPresetById`; URL: `coordinateUrlInstallPreflight`; install progress: `ModelInstallActivityKind` + `model_install_status_copy` (plugin only reports 0–100%); error **Retry download** (`lastFailedDownloadUrl`); `model_registry_snapshotProvider`
- dashboard-model-ui: `_NoOfflineModelBanner` — same status copy as settings; `coordinateInferenceNetworkInstallPreflight` + `installPresetById(kSmallestStoragePresetId)`; rest unchanged
- auth: **no login** — `HomeRoute` `/` initial; no `AuthGuard`/`LoginRoute`; drawer has no logout; drawer header: gradient + centered `logo.svg` (`svg_flutter`) tinted `onPrimary` srcIn; `AppAppBar` title: `logo-top-bw.svg` tinted app bar fg / `onSurface`; **no Get Photo** (`image_picker` removed)
- ux-home: `appAppearanceProvider` + 4 themes; default **dark high contrast**; `IndexedStack` home tabs; `homeBottomNavIndexProvider` syncs bottom nav for chat context refresh; Wiki `wiki_pack.json` (incl. `karpa_cpr`, `morse_code` GFM tables); todos/chat/wiki as before; dashboard: status card `ExpansionTile` (subtitle: device + conditions) → expanded: device rows + **Day / night** (`titleSmall`) + compact **Conditions** `DashboardWeatherCard` (sun up/down one line, Typical temps + Data source; no ~ before °C/km (minus ambiguity); `dashboardWeatherProvider` + `light_state_calculator` + `climate_normals`); pull-to-refresh invalidates device + weather; quick actions: Call help, CPR, SOS, Planning last (`DashboardActionCard`; grid `childAspectRatio` **3/2**; compact avatar/text); SOS overlay (`sos_overlay`): bottom sheet UI — **Morse** row (live ·/― highlight); `TextField` CAPS A–Z0–9 + space (`_MorseCapsFormatter`); `_speed` Slider → `_unitMs` (500..72ms dit) + ~WPM; `_MorseToken` seq `compute(_sosBuildSequenceForCompute)` + `_sequenceBuildTicket` stale-cancel + `_runLoop` index + wrap `_delayWithPause((_unitMs*12).clamp(650..3200))`; Pause/Resume/Stop (reset idx); Start/Close; FilterChip BT/Audio/Light/Vibe (`_lightAlerts` gates torch+screen flash); torch+screen + `flutter_soloud` sine (`sos_morse_tone.dart`; tone start then torch; single delay + vibe `Future.wait`; stop tone in finally) + `vibration`/haptic Morse-sync marks; settings: drawer → `AppearanceSettingsRoute` + `ModelConfigRoute` (`ModelSetupWidget`); HC banner (dashboard): **Light mode** → `setMode(light)` (banner hides); dismiss ✕; reappears after switching away from dark HC
- local-model: `ModelInstallOrchestrator` + `ModelRegistryStore` (JSON v2; migrate from `model_install_prefs`); `localGemmaModelProvider`: install / `switchToRegistryEntry` / `removeRegistryEntry` / `exportRegistryEntryToUserLocation` / `installPresetById`; `model_file_export` (`FilePicker.saveFile` + bytes); cold-start `ColdStartRestoreResult` + `onRestoreBegins`; `mapModelInstallException`; `InferenceModelDescriptor`; `model_install_flow_coordinator`; legacy prefs one-time migration only
- chat: `GemmaLocalService` + `ChatRuntimeGateway`; system instruction = `kDisasterSystemInstruction` + `composeDisasterSystemInstruction` (`chat_dashboard_context`) on init; entering **Chat tab** from elsewhere (`homeBottomNavIndexProvider`) re-samples dashboard + `setDeviceContextForNextUserMessage` (prepends refresh block on next user turn — avoids `createChat` vs UI history mismatch); first load / reset: `reloadInferenceWeights`; `[[TODOS]]` → `stripTodosAndApply` + `todoTabBadgeProvider`
- router: `appRouter` `@riverpod` functional ref lint fixed: removed typedef; signature now `appRouter(Ref ref)`
- deps: connectivity_plus; file_picker; flutter_secure_storage; riverpod 3.x + hooks; auto_route 11; melos 7; sdk >=3.5 (`flutter pub upgrade --major-versions` May 2026); **flutter_soloud** (SOS Morse sine; init `bufferSize: 4096`); **audio_session** (SOS: alarm/sonification + transient exclusive focus Android; playback+speaker iOS; static refcount restore on dispose); **vibration** (SOS Morse-sync); **path** (join app docs for model restore)
- ios: Podfile platform 16, `use_frameworks! :linkage => :static`; Info.plist file sharing keys; display name Disastron
- docs: root `README.md` + backlog `future-features.md`; no other checked-in `.md`
