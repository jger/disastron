- readme: name etymology (δις + ἄστρον ↔ disaster via Latin dis- + astrum / Italian disastro); features + mission; open-source-when-ready; tech blurb
- android-gradle: Gradle 8.13, AGP 8.13.1, Kotlin 2.3.20; app JVM 17; app `compileSdk 36`; subprojects library `compileSdk 36` (androidx.core 1.17 / background_downloader)
- android-r8: `app/proguard-rules.pro` `-dontwarn` MediaPipe proto stubs (release R8; matches AGP `missing_rules.txt`)
- android-16kb: `ndkVersion 28.2.13676358`; manifest `android:pageSizeCompat=enabled`; APK check: offending ELF `libLiteRtLm.so` (`max_load_align=0x1000`) from flutter_gemma native assets -> runs in compat mode until upstream 16KB-aligned build
- flutter_gemma: ^0.14.1; main `FlutterGemma.initialize(huggingFaceToken: prefs)` when saved; no bundled `model.bin` (assets/images only)
- hf-token: `HuggingfaceToken.save`/`clear` → `FlutterGemma.initialize(huggingFaceToken: …)` so downloads see token; `installFromNetwork` always passes saved/override token (Qwen + Gemma)
- presets: `predefined_models` — **Qwen first** (2.5 0.5B `.task`, 3.5 0.8B `.litertlm` LudwigBanach + `ModelType.qwen3`); Gemma block below; `kSmallestStorageDownloadPreset` = Gemma 270M q8 (`kSmallestStoragePresetId`)
- model-ui: `ModelSetupWidget` — no duplicate page title (AppBar `Offline model` on `ModelConfigPage` only); presets (Qwen/Gemma) **hidden until saved HF token**; saved token UI = **Clear token** only (no field/Save); **From URL** = URL field + download only (model type `modelTypeForInferenceUrl`; saved HF token used if set; no token gate on that tab); preset tab keeps token + `showHuggingFaceTokenPasteDialog` path via `_installPreset`; `model_network_install` + `connectivity_plus`; dashboard smallest CTA dialog
- dashboard-model-ui: `_NoOfflineModelBanner` on `DashboardHomeBody` when `localGemmaPhase.notInstalled` — CTA download smallest + link `ModelConfigRoute`; empty state + **TabBar** (Download preset / This device / From URL); installed shows `readActiveInferenceSummary()` + "Change model" replace flow
- auth: **no login** — `HomeRoute` `/` initial; no `AuthGuard`/`LoginRoute`; drawer has no logout; drawer header: gradient + centered logo SVG (`svg_flutter`) tinted `onPrimary` srcIn; no duplicate title (wordmark in SVG); **no Get Photo** (`image_picker` removed)
- ux-home: `appAppearanceProvider` + 4 themes; default **dark high contrast**; `IndexedStack` home tabs; Wiki `wiki_pack.json` (incl. `karpa_cpr`, `morse_code` GFM tables); todos/chat/wiki as before; dashboard: status card `ExpansionTile` (subtitle: device + conditions) → expanded: device rows + **Day / night** (`titleSmall`) + compact **Conditions** `DashboardWeatherCard` (sun up/down one line, Typical temps + Data source; no ~ before °C/km (minus ambiguity); `dashboardWeatherProvider` + `light_state_calculator` + `climate_normals`); pull-to-refresh invalidates device + weather; quick actions: Call help, CPR, SOS, Planning last (`DashboardActionCard`; grid `childAspectRatio` **3/2**; compact avatar/text); SOS overlay (`sos_overlay`): bottom sheet UI — **Morse** row (live ·/― highlight); `TextField` CAPS A–Z0–9 + space (`_MorseCapsFormatter`); `_speed` Slider → `_unitMs` + ~WPM; `_MorseToken` seq `compute(_sosBuildSequenceForCompute)` + `_sequenceBuildTicket` stale-cancel + `_runLoop` index + wrap `_delayWithPause((_unitMs*12).clamp(650..3200))`; Pause/Resume/Stop (reset idx); Start/Close; FilterChip BT/Audio/Vibe (BT on → SnackBar); torch+screen + `audio_session` + dual `just_audio` (`morse_dit`/`morse_dah`; Android `setAndroidAudioAttributes` assistanceSonification vs alarm + `androidApplyAudioAttributes: false`) + `vibration`/haptic Morse-sync marks; settings: drawer → `AppearanceSettingsRoute` + `ModelConfigRoute` (`ModelSetupWidget`); HC banner (dashboard): dismiss ✕; Row crossAxis center; reappears after switching away from dark HC
- local-model: `local_gemma_model_provider` (keepAlive) `.fromFile` / `.fromNetwork`; **`model_install_prefs`** saves url/path + `ModelType`/`ModelFileType` after success; **`_tryRestoreModel`** on cold start (non-web): `isModelInstalled` + `fromFile` app docs dir → re-`setActiveModel` w/o re-download; `refreshFromEngine` skips if `installing` (avoid race w/ chat init); **403**: `isGated403` + `gatedModelPageUrl` → `model_setup_widget` SnackBar + `launchUrl` Open model page
- chat: `GemmaLocalService` + `InferenceChat` / `ModelResponse` streaming; disaster systemInstruction; `clearTfliteXnnpackWeightCaches()` before `getActiveModel` (mitigate TFLite XNNPACK mmap cache append crash; 16k-page AVD may still need upstream fix / 4k emulator); **empty assistant body** after `[[TODOS]]` strip → placeholder `(Checklist updated.)` (no eternal spinner)
- router: `appRouter` `@riverpod` functional ref lint fixed: removed typedef; signature now `appRouter(Ref ref)`
- deps: connectivity_plus; file_picker; flutter_secure_storage; riverpod 3.x + hooks; auto_route 11; melos 7; sdk >=3.5 (`flutter pub upgrade --major-versions` May 2026); **just_audio**, **audio_session**, **vibration** (SOS Morse-sync); **path** (join app docs for model restore)
- ios: Podfile platform 16, `use_frameworks! :linkage => :static`; Info.plist file sharing keys; display name Disastron
- docs: root `README.md` only (features, mission, open-source-when-ready); no other checked-in `.md`
